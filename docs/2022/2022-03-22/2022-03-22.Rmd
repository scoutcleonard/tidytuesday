---
title: "tidy tuesday 2022-03-22"
author: "Scout Leonard"
date: "3/22/2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Load Libraries 

```{r}
if (!require(librarian)){
  install.packages("librarian")
  library(librarian)
}
librarian::shelf(tidyverse,
                 here,
                 MetBrewer)
```

# Read in Data

```{r}
babynames <- readr::read_csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2022/2022-03-22/babynames.csv')
```

```{r}
scout <- babynames %>% 
  filter(name == "Scout") %>% 
  filter(sex == "F")
```

```{r}
# font_add_google("EB Garamond", "serif")
# showtext_auto()
```

```{r}
# book <- glue("The novel, 'To Kill a Mockingbird,'",
#              "by Harper Lee, was published in 1960.")
# 
# movie <- glue("The film version was released in 1962.")
# 
# celeb <- glue("Bruce Willis and Demi Moore name their baby girl",
#               "'Scout' in 1992.")
```


```{r}
ggplot(data = scout, aes(x = year, y = n)) +
  geom_line(color = "#41485f") + 
  geom_vline(xintercept = 1960, color = "#9b332b") +
  geom_vline(xintercept = 1962, color = "#b64f32") +
  geom_vline(xintercept = 1991, color = "#d39a2d") +
  theme_minimal() +
  labs(title = "From Haper Lee to Demi Moore:",
       subtitle = "How did Scouts of popular culture influence U.S. baby names?",
       x = "Year",
       y = "Babies Named Scout") +
  theme(plot.margin = margin(rep(15, 4)),
        plot.background = element_rect(fill = "#b9b9b8"),
        panel.grid = element_line(color = "#b9b9b8"),
        plot.title = element_text(size = 20, hjust = 0.5, color = "#41485f"),
        plot.subtitle = element_text(hjust = 0.5, color = "#41485f"),
        axis.title = element_text(color = "#41485f")) +
  geom_text(x = 1963, 
            y = 225, 
            label = "The novel, 'To Kill a Mockingbird \nby Harper Lee, was published in 1960.", 
            color = "#9b332b",
            hjust = 0, 
            size = 3) +
  geom_text(x = 1963, 
            y = 195, 
            label = "The film version followed shortly \n after in 1962.",
            color = "#b64f32",
            size = 3,
            hjust = 0) +
  geom_text(x = 1992,
            y = 195,
            label = "In 1991, Demi Moore and Bruce Willis \nname their baby 'Scout.'",
            color = "#d39a2d",
            size = 3,
            hjust = 0)
```

